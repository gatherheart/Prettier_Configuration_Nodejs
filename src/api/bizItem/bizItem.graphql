type BizItemResponse {
  error: Boolean!
  errorMessage: String
  bizItem: BizItem
}

type BizItem {
  businessId: String
  bizItemId: String
  slotMapId: [String]
}

type Query {
  getBizItemInfo(bizItemId: String!): BizItemResponse!
}

type Mutation {
  sample1(channel: String!, text: String!): SampleMessage!
}
